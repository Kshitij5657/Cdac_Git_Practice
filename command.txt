git init =>to initialise git on project

git add -A  to add files to staged state (now git tracking all file)

git commit -m "write message"

git branch

git log show all commits date time and messages

git diff

git merge
